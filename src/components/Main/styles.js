import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  root: {
    borderRadius: 15,
  },
  divider: {
    margin: "1em 0",
  },
  cardContent: {
    paddingTop: 0,
  },
  total: {
    fontWeight: 500
  }
}));